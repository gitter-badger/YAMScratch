function [alpha] = mdoZoomStage(al_low,al_high,f_low,f_high)
%Purpose: returns
% al_low - the step size for which phi is smaller
% al_high - the step size for which phi is larger
% f_low - cached function evaluation
% f_high - cached function evaluation

	%find a trial point between al_low and al_high
	%

	return
end